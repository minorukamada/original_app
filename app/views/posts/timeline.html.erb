<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="pt-3 pb-3 mt-3 mb-3">
      <ul class="nav nav-tabs nav-justified" id="user_items">
        <li class="nav-item"><a href="<%= posts_path %>" class="nav-link <%= 'active' if current_page?(posts_path) %>">新着投稿</a></li>
        <li class="nav-item"><a href="<%= timeline_post_path(current_user) %>" class="nav-link <%= 'active' if current_page?(timeline_post_path(current_user)) %>">タイムライン</a></li>
        <li class="nav-item"><a href="<%= new_user_path %>" class="nav-link <%= 'active' if current_page?(new_user_path) %>">タイムライン</a></li>
      </ul>
      <%= form_tag(posts_path, :method => 'get') do %>
        <div class="d-flex justify-content-center pt-4">
          <%= text_field_tag :search, '', class: 'title-search form-control', :placeholder => "タイトル検索"%>
          <%= button_tag :type => "submit", :class =>"btn btn-default btn-dark" do %>
            <i class="fa fa-search"></i>
          <% end %>
        </div>
      <% end %>
    </div>
    <div>
      <% if @posts.count > 0  %>
        <%= render 'posts/posts', posts: @posts %>
      <% else %>
        <p>該当する投稿が見つかりませんでした。</p>
      <% end %>
    </div>
  </div>
</div>
<div class="container">
  <h1><%= @user.name %>の詳細ページ</h1>
  <%= render 'relationships/follow_button', user: @user %>
  <%= link_to 'Followings', followings_user_path(@user) %><span class="badge badge-secondary"><%= @count_followings %></span>
  <%= link_to 'Followers', followers_user_path(@user) %><span class="badge badge-secondary"><%= @count_followers %></span>
  
  <% if current_user == @user %>
    <div class="btn btn-light">
      <%=link_to 'ユーザー情報を編集する', edit_user_path(@user) %>
    </div>
  <% end %>
  <p>過去の投稿</p>
  <%= render 'posts/posts', posts: @posts %>
  
  
</div>
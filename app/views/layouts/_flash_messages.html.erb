<div class="flash_messages fixed-top row justify-content-center">
  <div class="flash_message col-md-6">
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
  </div>
</div>
<nav class="navbar navbar-expand-sm navbar-dark bg-secondary sticky-top">
  <a class="navbar-brand" href="/">Minimal Life</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto"></ul>
    <ul class="navbar-nav">
      <% if logged_in? %>
        <li class="nav-item dropdown">
          <li class="nav-item"><span class="nav-link"><i class="fas fa-bookmark fa-fw"></i><%= link_to 'お気に入り一覧', likes_user_path(current_user) %></span></li>
          <li class="nav-item"><span class="nav-link"><i class="fas fa-pen fa-fw"></i><%= link_to '新規投稿', new_post_path %></span></li>
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><i class="fas fa-user fa-fw"></i><%= current_user.name %></a>
          <ul class="dropdown-menu dropdown-menu-right">
            <li class="dropdown-item"><%= link_to 'My profile', user_path(current_user) %></li>
            <li class="dropdown-divider"></li>
            <li class="dropdown-item"><%= link_to 'Logout', logout_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li class="nav-item"><%= link_to '新規登録', signup_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'ログイン', login_path, class: 'nav-link' %></li>
      <% end %>
    </ul>
  </div>
</nav>
  
  
